# Server Environment Variables (.env)
PORT=5000
NODE_ENV=development

# Database
DATABASE_URL="your-database-url"

# JWT Configuration - Use strong, random secrets in production
JWT_SECRET="your-super-secure-jwt-secret-at-least-32-characters-long"
JWT_REFRESH_SECRET="your-different-super-secure-refresh-secret-at-least-32-characters-long"

# Token Expiration
JWT_ACCESS_TOKEN_EXPIRATION=15m
JWT_REFRESH_TOKEN_EXPIRATION=7d
JWT_EXPIRATION=1h

# Production Security Notes:
# 1. JWT_SECRET and JWT_REFRESH_SECRET should be different and at least 32 characters
# 2. Use a cryptographically secure random generator for secrets
# 3. Never commit these secrets to version control
# 4. Use environment variable management tools in production
# 5. Rotate secrets periodically
